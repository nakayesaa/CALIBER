{
  "name": "caliber",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "start": "bash scripts/start-dev.sh",
    "dev": "npm run dev --workspace=@caliber/web",
    "build": "npm run build --workspace=@caliber/web",
    "preview": "npm run preview --workspace=@caliber/web",
    "check": "npm run check --workspace=@caliber/web"
  },
  "engines": {
    "node": ">=20.19"
  }
}
